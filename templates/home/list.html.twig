{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <div class="d-flex justify-content-between">
        <div>
           Arnaud  Yanga
        </div>

        <div>
            <a href="{{ path('app_logout') }}"><p class="card-title">Déconnexion</p></a>
        </div>

    </div>
<div class="example-wrapper">

    <div class="album py-5" id="department">
        <div class="container">
            <h1>List des commentaires</h1>

            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
                {% for comments  in list %}
                    <div class="col">
                        <div class="card shadow-lg h-100 m-2">
                            <div class="card-body">
                                <a href="{{ path('comments_detail', {id: comments.id }) }}"><h5 class="card-title">{{ comments.text }}</h5></a>
                                <p class="card-text">Note : <span class="badge bg-secondary">{{ comments.note }}</span></p>
                                <p class="card-text">Date de creation : <span class="badge bg-secondary">{{ comments.createdAt }}</span></p>
                                {% if  comments.user.firstname is defined and comments.user.firstname is defined %}
                                <p class="card-text">Auteur : <span class="badge bg-secondary">{{ comments.user.firstname}} {{ comments.user.lastname}}  </span></p>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

</div>
{% endblock %}
